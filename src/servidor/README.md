# Módulo servidor

Esta carpeta contendrá en el futuro la configuración principal del servidor backend de MicroFlux.

Aquí se ubicará el archivo encargado de iniciar Fastify, configurar el puerto del servidor, registrar rutas principales y preparar la aplicación para recibir peticiones HTTP.

En esta etapa no se implementa el servidor funcional, solo se deja preparada la estructura.